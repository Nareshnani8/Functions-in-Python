def image():
    '''Function to read image file and display as output'''
    
    import PIL
    from PIL import Image
    img = Image.open(filename,'r')
    
    return display(img)

file_name = input('Please enter the image file name with .jpg extension')

image(file_name)
